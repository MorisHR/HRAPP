# ============================================================================
# HRMS APPLICATION SECRETS - PRODUCTION CONFIGURATION
# ============================================================================
#
# This file documents all required environment variables for production.
# NEVER commit actual secrets to version control!
#
# For local development:
#   1. Copy this file to .env
#   2. Fill in the values
#   3. Use dotnet user-secrets for sensitive data
#
# For production:
#   1. Set these in your deployment environment
#   2. Use Google Secret Manager, AWS Secrets Manager, or Azure Key Vault
#   3. Never use the development fallback values
#
# ============================================================================

# ----------------------------------------------------------------------------
# CRITICAL SECRETS (Required - Application will not start without these)
# ----------------------------------------------------------------------------

# JWT Secret for token signing - MUST be 32+ characters
# Generate with: openssl rand -base64 48
# Example: JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-required-for-security
JWT_SECRET=

# Encryption key for PII data (AES-256) - MUST be 32+ characters
# Generate with: openssl rand -base64 48
# Example: ENCRYPTION_KEY=your-encryption-key-32-chars-minimum-for-aes256-encryption
ENCRYPTION_KEY=

# SuperAdmin secret login path (UUID recommended)
# Generate with: uuidgen (Linux/Mac) or New-Guid (PowerShell)
# Example: SUPERADMIN_SECRET_PATH=a1b2c3d4-e5f6-7890-abcd-ef1234567890
SUPERADMIN_SECRET_PATH=

# Database password
# Example: DB_PASSWORD=your-secure-database-password
DB_PASSWORD=

# ----------------------------------------------------------------------------
# OPTIONAL SECRETS (Application works without these, but features are limited)
# ----------------------------------------------------------------------------

# SMTP Password for email service (SendGrid, Google Workspace, etc.)
# If not set: Email functionality will be disabled
# Example: SMTP_PASSWORD=your-smtp-password-or-api-key
SMTP_PASSWORD=

# Redis connection string for distributed caching
# If not set: Will use in-memory caching only (not recommended for production)
# Format: localhost:6379,password=yourpassword,ssl=True,abortConnect=False
# Example: REDIS_CONNECTION_STRING=your-redis-host:6379,password=yourpassword
REDIS_CONNECTION_STRING=

# SIEM API key for security logging (Splunk, Datadog, etc.)
# If not set: SIEM logging will be disabled
# Example: SIEM_API_KEY=your-siem-api-key
SIEM_API_KEY=

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# ----------------------------------------------------------------------------

# Database connection string (password comes from DB_PASSWORD above)
# Example: DB_HOST=localhost
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hrms_master
DB_USERNAME=postgres

# ----------------------------------------------------------------------------
# APPLICATION ENVIRONMENT
# ----------------------------------------------------------------------------

# Environment: Development, Staging, Production
ASPNETCORE_ENVIRONMENT=Production

# ----------------------------------------------------------------------------
# SMTP CONFIGURATION
# ----------------------------------------------------------------------------

# SMTP server settings (password from SMTP_PASSWORD above)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=noreply@yourcompany.com
SMTP_FROM_NAME=HRMS System

# ----------------------------------------------------------------------------
# REDIS CONFIGURATION
# ----------------------------------------------------------------------------

# Redis settings (connection string from REDIS_CONNECTION_STRING above)
REDIS_ENABLED=true
REDIS_INSTANCE_NAME=hrms

# ----------------------------------------------------------------------------
# CORS CONFIGURATION (Production domains)
# ----------------------------------------------------------------------------

# Comma-separated list of allowed origins
# Example: CORS_ORIGINS=https://yourapp.com,https://www.yourapp.com
CORS_ORIGINS=

# ----------------------------------------------------------------------------
# GOOGLE CLOUD CONFIGURATION (If using GCP)
# ----------------------------------------------------------------------------

# Google Cloud Project ID
GCP_PROJECT_ID=

# Google Cloud Storage bucket for backups
GCS_BACKUP_BUCKET=

# Google Secret Manager (if using instead of environment variables)
# When enabled, secrets will be loaded from Secret Manager
USE_GOOGLE_SECRET_MANAGER=false

# ----------------------------------------------------------------------------
# MONITORING & LOGGING
# ----------------------------------------------------------------------------

# Application Insights instrumentation key (Azure)
APPINSIGHTS_INSTRUMENTATIONKEY=

# Google Cloud Logging (GCP)
GOOGLE_CLOUD_LOGGING_ENABLED=false

# Log level: Trace, Debug, Information, Warning, Error, Critical
LOG_LEVEL=Information

# ----------------------------------------------------------------------------
# RATE LIMITING
# ----------------------------------------------------------------------------

# Global rate limit (requests per minute per IP)
RATE_LIMIT_PER_MINUTE=1000

# API key for unlimited rate limit (SuperAdmin operations)
RATE_LIMIT_BYPASS_KEY=

# ----------------------------------------------------------------------------
# SECURITY HEADERS
# ----------------------------------------------------------------------------

# Content Security Policy
CSP_ENABLED=true

# Strict Transport Security max age (seconds)
HSTS_MAX_AGE=31536000

# ----------------------------------------------------------------------------
# FEATURE FLAGS
# ----------------------------------------------------------------------------

# Enable experimental features
ENABLE_EXPERIMENTAL_FEATURES=false

# Enable background job processing
ENABLE_BACKGROUND_JOBS=true

# Enable Redis caching (requires REDIS_CONNECTION_STRING)
ENABLE_REDIS_CACHE=true

# Enable MFA for tenant admins
ENFORCE_TENANT_ADMIN_MFA=false

# ============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================================================
#
# Before deploying to production, ensure:
#
#  [ ] All CRITICAL SECRETS are set with strong, random values
#  [ ] JWT_SECRET is at least 48 characters (64+ recommended)
#  [ ] ENCRYPTION_KEY is at least 48 characters (64+ recommended)
#  [ ] SUPERADMIN_SECRET_PATH is a unique UUID (not the example value)
#  [ ] DB_PASSWORD is a strong password (16+ characters)
#  [ ] SMTP_PASSWORD is set (if email is required)
#  [ ] REDIS_CONNECTION_STRING is set (for production scale)
#  [ ] CORS_ORIGINS is set to your production domain(s)
#  [ ] ASPNETCORE_ENVIRONMENT is set to "Production"
#  [ ] LOG_LEVEL is set to "Information" or "Warning" (not "Debug")
#  [ ] All secrets are stored in Secret Manager (not hardcoded)
#  [ ] .env file is added to .gitignore
#  [ ] Database backups are configured
#  [ ] Monitoring and alerting are configured
#
# ============================================================================
